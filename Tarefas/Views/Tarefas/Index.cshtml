@model TarefaViewModel;

@{    
    ViewData["Title"] = "Gerenciar Lista de Tarefa";
    }

<div class="panel panel-default tarefa-panel" >
    <div class="panel-heading">@ViewData["Title"]</div>

    <table class="table table-hover" >
        <thead>
            <tr>
                <td>&#x2714;</td>
                <td>Tarefa</td>
                <td>Previsão</td>
            </tr>
        </thead>

        @foreach (var item in Model.TarefasItem)
        {
            <tr>
                <td>
                    <input type="checkbox" name="@item.Id" readonly checked="@item.isCompleta" value="true" class="done-checkbox"/>
                 </td>
                <td>@item.Nome</td>
                <td>@String.Format("{0:dd/MM/yyyy}",@item.DataConclusao)</td>
                <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Editar</a> |
                 <a asp-action="Delete" asp-route-id="@item.Id">Deletar</a>
                  </td>
            </tr>
        }
    </table>
    <div class="panel-footer add-item-form">
           <form asp-controller="Tarefas" asp-action="AdicionarItemTarefa" method="GET">                     
               <button type="submit" class="btn btn-success">Incluir Nova Tarefa</button>
           </form>
    </div>
</div>